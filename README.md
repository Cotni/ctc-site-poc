# Track Council Website

To test locally, ensure the environment variable `MONGO_URL` is set to a correct MongoDB URL, run `ng build --output-path dist` to build the web app, and serve with `npm run devstart`.